class Player{
	private:
		int id;
		std::string name;
		int score;
	public:
		Player(int id, std::string name);
		void set_id(int id);
		int get_id();
		void set_name(std::string name);
		std::string get_name;
}
